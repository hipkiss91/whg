export type RegisterForm = {
    id: string
    name: string
    email: string
    password: string
    confirmPassword: string
    dateOfBirth: string
    service: string
    acceptTerms: boolean
};
